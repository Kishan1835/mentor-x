import React from "react";

const Mainlayout = ({ children }) => {
  // Redirect to onBoarding

  return <div className="contaioner mx-auto mt-24 mb-20">{children}</div>;
};

export default Mainlayout;
